# miterlab-v3

Photography-style portfolio (Vite + React + Tailwind + React Router), dark theme,
JetBrains Mono. Deployed to GitHub Pages.

```bash
npm install
npm run dev      # http://127.0.0.1:5174
npm run build
```
